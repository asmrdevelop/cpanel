<div id="{{ zone_rec.id_prefix }}_detail_{{ zone_rec._id }}">
    <div ng-repeat="part in zone_rec.txtdata track by $index">
        {{ part }}
        <hr class="txtSeparator" ng-if="zone_rec.txtdata.length > 1 && !$last"/>
    </div>
</div>