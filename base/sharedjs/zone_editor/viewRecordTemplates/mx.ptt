<div id="mx_detail_{{ zone_rec._id }}">
    <span class="record-property-label">[% locale.maketext('Priority:') %] </span>{{ zone_rec.priority }}<br>
    <span class="record-property-label">[% locale.maketext('Destination:') %] </span>{{ zone_rec.exchange }}
</div>