<div id="ds_detail_{{ zone_rec._id }}">
    <span class="record-property-label">[% locale.maketext('Key Tag:') %] </span>{{ zone_rec.keytag }}<br>
    <span class="record-property-label">[% locale.maketext('Algorithm:') %] </span>{{ zone_rec.algorithm }}<br>
    <span class="record-property-label">[% locale.maketext('Digest Type:') %] </span>{{ zone_rec.digtype }}<br>
    <span class="record-property-label">[% locale.maketext('Digest:') %] </span>{{ zone_rec.digest }}
</div>