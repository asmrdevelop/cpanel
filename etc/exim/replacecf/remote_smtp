remote_smtp:
  driver = smtp
  # lookup in order of .. forwarded address (original_domain), current sender (sender_address_domain), current uid (perl function get_sender_uid), then revert to default
  interface = <; %CPANEL-smtp-interface%
  helo_data = %CPANEL-helo-data%
  hosts_try_chunking = 198.51.100.1
  message_linelength_limit = [% exim_config.message_linelength_limit %]

