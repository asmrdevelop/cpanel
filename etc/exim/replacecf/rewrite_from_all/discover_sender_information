#
# For from rewrites - All Version
# Prefills information about the sender and makes sure
# we do a lookup if it was a non local domain
#
#  This condition will always be false, we just need
#  to run the perl code at this stage with the
#  permission to read /etc/userdomains
#

discover_sender_information:
    condition = "${perl{discover_sender_information}}"
    driver = redirect
    user = mailnull
    data = ""
    no_verify
