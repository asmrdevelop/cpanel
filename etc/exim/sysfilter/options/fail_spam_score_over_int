
# The spam score is the "int" value.  For example a spam score of 2.5 would be 25
# and a spam score of 5.0 would be 50

if ($h_x-spam-score: matches \N^\d+$\N and $h_x-spam-score: is above [% VALUE %])
then
    fail text "The mail server detected your message as spam and has prevented delivery ([% VALUE %])."
endif

