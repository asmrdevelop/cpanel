
  # Support for scanning secondarymx domains

  warn  domains = ! +local_domains : +secondarymx_domains
         condition = ${if <= {$message_size}{[% ACL_MAX_SPAM_SCAN_SIZE %]K}{1}{0}}
          set acl_m0    = 1
          set acl_m1    = [% ACL_EXIMSCANUSER %]


