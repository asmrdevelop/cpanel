[%
    IF NOTICE.down_hosts.size > 1;
      CPANEL.locale.maketext('Unreachable DNS Cluster Members.');
    ELSE;
      CPANEL.locale.maketext('Unreachable DNS Cluster Member “[_1]”.', NOTICE.down_hosts.0);
    END;
%]
