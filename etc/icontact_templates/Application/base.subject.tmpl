[%
    SET level_loc =
        (NOTICE.level == 'warn') ? CPANEL.locale.maketext('Warning')
        : (NOTICE.level == 'error') ? CPANEL.locale.maketext('Error')
        : 'UNKNOWN'
    ;
-%]
[% level_loc %] - [% NOTICE.application_name %]
