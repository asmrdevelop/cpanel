[%
    PROCESS "include/colors.tmpl";
    PROCESS "include/styles.tmpl";

    WRAPPER 'wrapper/main.html.tmpl';

    NOTICE.email_reason = CPANEL.locale.maketext('You must reboot the server to enable quotas.');

%]
<p>
    [% CPANEL.locale.maketext("The system will enable filesystem quotas the next time that you reboot the server."); %]
</p>
<p>
    [% CPANEL.locale.maketext('The top right corner of the [asis,WHM] interface will display a reboot message until you reboot the server.'); %]
</p>
<p>
    [% CPANEL.locale.maketext('After you reboot the server, the system will perform a one-time filesystem scan to initialize the quota system.'); %] [% CPANEL.locale.maketext('This scan may require a long amount of time if it must scan a large amount of data.'); %] [% CPANEL.locale.maketext('While the scan runs, do not create, transfer, or restore new user accounts.') %] [% CPANEL.locale.maketext('The system will send a final notification when the quota initialization scan finishes.') %]
</p>
[% END %]
