
package File::GlobMapper;

#this is stub to lower memory usage since we never use File::Glob with IO::Compress

no warnings "redefine";

sub new {
    my $self = {};
    bless $self;
    return $self;
}
sub globmap { }

1;
