## This module is automatically generated

package cPAddonsConf;

our %inst = (

[% FOREACH kv = data.config_definitions;
        SET module_name = kv.key;
        SET addon_info  = kv.value;
-%]
    '[% module_name -%]' => {
        version          => '[% addon_info.version -%]',
        VERSION          => '[% addon_info.VERSION -%]',
        is_rpm           => '[% addon_info.is_rpm -%]',
        display_app_name => '[% addon_info.display_app_name -%]',
        desc             => '[% addon_info.desc -%]',
        deprecated       => '[% addon_info.deprecated -%]',
    },
[% END -%]

);

our %vend = (

[% FOREACH kv = data.ownvend;
    SET vendor = kv.key;
    SET vendor_info  = kv.value;
-%]
    '[% vendor -%]' => {
        vndinf => '[% vendor_info.vndinf -%]',
        cphost => '[% vendor_info.cphost -%]',
        cphuri => '[% vendor_info.cphuri -%]',
        palmd5 => '[% vendor_info.palmd5 -%]',
    },
[% END -%]

);

1;
