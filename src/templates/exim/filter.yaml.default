---
filter:
  -
    actions:
      -
        action: save
        dest: __JUNKDIR__
    filtername: Spam
    rules:
      -
        match: is above
        opt: or
        part: "$h_X-Spam-Score:"
        val: 10
